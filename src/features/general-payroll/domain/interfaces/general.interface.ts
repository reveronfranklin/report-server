export interface IGeneral {
  conceptType: string;
  conceptNumber: string;
  conceptDenomination: string;
  assignment: number;
  deduction: number;
  visibleAmount: number;
  amount: number;
  deductible: number;
}